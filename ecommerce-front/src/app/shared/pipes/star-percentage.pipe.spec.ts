import { StarPercentagePipe } from './star-percentage.pipe';

describe('StarPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new StarPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
